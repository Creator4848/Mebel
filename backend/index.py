from app.main import app

# This file acts as the explicit entrypoint for Vercel's Python serverless function.
# By placing it in the /backend directory alongside requirements.txt, Vercel correctly
# resolves the dependency tree and the 'app' module imports.
